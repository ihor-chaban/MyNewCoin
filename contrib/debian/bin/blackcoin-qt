#!/bin/sh

set -e

umask 077

basedir=~/.bcca
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/bcca.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# BCCA does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/bcca/bcca-qt "$@"
